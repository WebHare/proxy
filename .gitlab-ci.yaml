stages:
  - build

variables:
  TAG: gitlab-registry.b-lex.com/$CI_PROJECT_PATH:$CI_BUILD_REF_NAME

before_script:
  - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN gitlab-registry.b-lex.com

buildmachine:
  stage: build
  script:
    - ./docker.sh
    - docker push $TAG
